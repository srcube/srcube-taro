{
  "name": "@srcube-taro/utils-tv",
  "version": "0.0.3",
  "author": "chioio <srcube.chioio@gmail.com>",
  "keywords": [
    "taro.js",
    "tailwind-variants",
    "utils"
  ],
  "main": "src/index.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public",
    "main": "dist/index.js"
  },
  "scripts": {
    "build": "tsup --dts",
    "prepack": "rm -rf ./package && clean-publish",
    "postpack": "rm -rf ./package"
  },
  "dependencies": {
    "@weapp-core/escape": "catalog:",
    "@weapp-tailwindcss/merge": "catalog:",
    "tailwind-merge": "catalog:",
    "tailwind-variants": "catalog:"
  },
  "devDependencies": {
    "@srcube-taro/ts-config": "workspace:^"
  }
}
